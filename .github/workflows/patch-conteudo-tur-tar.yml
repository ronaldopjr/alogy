name: Patch conteudo tecnico TUR TAR

on:
  push:
    branches:
      - agent/conteudo-tecnico-tur-tar

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Aplicar conteudo tecnico
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          path = Path